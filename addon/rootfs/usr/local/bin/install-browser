#!/usr/bin/env bash
# Chromium is now pre-installed. This script is kept for backwards compatibility.
echo "Chromium is already installed. Both Playwright and agent-browser are ready to use."
echo "  PLAYWRIGHT_CHROMIUM_EXECUTABLE_PATH=$PLAYWRIGHT_CHROMIUM_EXECUTABLE_PATH"
echo "  AGENT_BROWSER_EXECUTABLE_PATH=$AGENT_BROWSER_EXECUTABLE_PATH"
